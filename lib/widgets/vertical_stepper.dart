import 'package:flutter/material.dart';

class VerticalStepper extends StatelessWidget {
  final int step;
  const VerticalStepper(this.step);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
