import 'package:flutter/material.dart';

class HorizontalStepper extends StatelessWidget {
  final int step;
  const HorizontalStepper(this.step);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
